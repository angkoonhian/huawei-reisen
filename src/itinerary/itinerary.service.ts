import { Injectable } from '@nestjs/common';
import { itineraryDTO } from './model/itinerary.dto';

@Injectable()
export class ItineraryService {
    public async getItineraryById(id: string) {
        try {
            
        } catch (err) {
            
        }
    }
}
